# 网页版永久删除

## 用户流程

在桌面 Chrome / Edge 等支持 File System Access API 的浏览器中打开应用，从“选择文件夹并扫描”进入。扫描本身只读取文件。结果页手动核对后点击“删除文件”，检查弹窗列出的每一个路径，输入“删除”并点击“确认”，允许浏览器目录写入授权后执行。

这是永久删除，不进入系统回收站，应用没有撤销功能。操作前先备份重要图片，操作期间勿在其他程序中编辑、移动或替换该目录的文件。相似图片可能包含独有内容，建议先逐张放大对比；弹窗不再提供额外的核对复选框，统一使用输入文字确认。

多选图片、降级目录上传入口和不支持的浏览器不提供真实删除。扫描结果仅在当前页面内存中临时保留，刷新或关闭后需要重新扫描；演示数据继续使用模拟流程。

## 安全边界

已按用户要求恢复单任务流程，不再提供“新建任务”、任务编号或独立标签页任务。新扫描替换当前内存结果，真实删除保留跨页面互斥锁。

不再保存或恢复扫描分组、图片预览、勾选状态和清理统计；不读写 IndexedDB / localStorage / sessionStorage，不导入旧缓存。本次移除功能不自动擦除历史缓存，更不会操作磁盘原图片。“已保存”“重试保存”“清除结果”和首页“继续查看结果”入口均已移除。

首页不再显示长篇目录授权/删除说明，独立的目录支持提示保留。删除确认弹窗中的真实文件清单、写入授权、不可撤销和不进入回收站等风险说明保持不变。

- 只处理弹窗确认的当前显示范围内的勾选，所有组至少留一份。
- 句柄只保留在当前页面内存，不写入缓存或服务器。不会按名称自动重新授权另一个目录。
- 删除前验证原目录内的解析路径、文件句柄身份、大小、修改时间和完整内容一致性；每次删除也重新验证所有保留项。完全一致副本采用下述完整字节核验，相似组仍逐文件计算 SHA-256。
- 仅调用父目录 `removeEntry(name, { recursive: false })` 删除文件，不创建条目，不清空目录，不使用永久删除的静默降级。
- Web Locks 防止同一来源并发删除，会话锁阻止重复提交、扫描替换和勾选变化。
- 删除不再依赖持久化中断标记。离开页面时取消扫描/删除令牌、释放预览和文件能力，返回或刷新只能重新扫描。删除中保留退出警告和停止按钮，已提交给系统的文件操作可能完成，不能撤销。
- 逐文件结果只保留在当前页面；仅成功项移除并计入统计，失败和取消项保留。发生意外异常时禁用进一步删除并提示重新扫描，不自动重试。页面关闭或崩溃后没有可恢复的明细，需要重扫核对磁盘现状。
- 浏览器 API 没有原子 compare-and-delete 保证，无法完全排除其他软件在检查和删除之间替换文件的竞态。文件大小合计也不等于操作系统实际释放空间（硬链接、云盘同步、缓存等可能影响）。

## 验证

运行 `npm run lint`、`npx vitest run`、`npm run build`。

测试覆盖：确认文字与灰色按钮校验提示、许可拒绝、当前范围与陈旧选择、全部类别保留项保护、文件变化/丢失/替换/移出目录、写权限撤销、部分失败、取消/重复提交、存储完全不可用时仍能工作、刷新为空、页面退出取消待授权/执行中的后续删除，以及异常后能力撤销。临时目录测试使用文件系统适配器真实删除合成文件，验证保留项与无关文件仍存在；它不等同于原生浏览器授权端到端测试。

## 删除吞吐与计时

- 同一任务最多 4 个独立组并行；同组删除仍串行。每一张删除前重新完整核验所有保留项和待删项，不缓存哈希或目录检查，不以大小/修改时间替代内容校验。
- 完整校验最多同时 4 个任务，输入数据预算 64 MiB；字节比较的两个输入同时计入预算，成对输入超预算则回退逐文件指纹校验，超过预算的单个文件独占。浏览器内部的复制和其他页面内存不计入这一预算。
- 取消与权限撤销会阻止新的系统删除调用；已经提交的调用可能完成。调用方会等待所有在途任务结束，再返回完整有序结果。
- 完成页“查看耗时明细”分别展示等待授权、权限检查、路径与文件检查、读取、内容校验和系统删除。分项累计时间存在并发重叠，不能相加或计算墙钟占比。此处信息仅存在本次页面内存，不上传、不保存。
- 使用 Node.js 24 运行 `node scripts/deletion-benchmark.mjs` 可比较串行基线和有限并行。基准只删除内存 Map 中的合成条目，不操作任何磁盘照片；每个文件系统调用人为增加 2ms 延迟，不能当作客户设备实测或速度保证。
- 新增测试覆盖跨组并行上限、同组完整重查、保留项同大小同修改时间变更、权限撤销/取消的全局停止、等候在途操作、输入预算、完成顺序稳定和聚合计时。

### 完全一致副本完整字节核验（2026-09-06，当前策略）

每次删除一个完全一致副本前，重新读取一个保留文件 K 和当前副本 T：校验 K 的完整 SHA-256 与扫描记录 H 一致，并比较 K、T 的全部字节，包括末尾不足四字节的部分；T 的扫描指纹也必须是 H。只有这些检查全部通过才可删除 T。即使 K、T 被同时改成相同内容且大小/修改时间不变，K 的新指纹仍会使操作失败。不抽样，不仅凭大小或修改时间批准删除。

同组额外保留文件仍逐一重读并完整计算指纹；每次删除后，下一次必须从新的文件读取开始，不复用上一次的保留文件缓冲或校验结果。高度相似、疑似相似和成对输入超预算的文件继续走逐文件完整 SHA-256。权限、路径、身份、至少留一份和停止规则不变，浏览器无法原子比对后删除的限制也不变。

沿用同一个最多四 Worker 的计算池。每个任务只做一次保留图指纹计算和一次副本完整比较，不为字节比较额外开线程池；Worker 自检增加相等、不相等和尾部差异检查。线程故障、缺失比较结果、非布尔结果或非法耗时一律失败，不用已分离缓冲重试。启动受限时使用主线程兼容实现，仍完整核验；不支持比较的旧 Worker 只走完整指纹路径。

完成页区分“完整指纹计算”和“副本完整字节比较”。完整核验总数、累计数据量仍包含每次保留项重读，指纹工作量减少并不等于 I/O 减少或总时间减半。

本机 Chromium 152，生产构建，13 组 / 195 个 6 MiB 合成副本，内存文件系统适配器；交替顺序各测六次：

| 方式 | 六次用时（秒） | 平均（秒） | SHA 次数 | 字节比较次数 | 输入峰值 |
| --- | --- | --- | --- | --- | --- |
| 原逐文件 SHA-256 | 0.445 / 0.462 / 0.459 / 0.466 / 0.477 / 0.470 | 0.463 | 390 | 0 | 24 MiB |
| 保留图 SHA-256 + 副本完整字节比较 | 0.480 / 0.441 / 0.414 / 0.404 / 0.431 / 0.416 | 0.431 | 195 | 195 | 48 MiB |

两种方式均完整核验 390 份输入 / 2340 MiB，195 个内存副本移除，13 个保留项仍存在。本机平均约快 7%，有单轮变慢，不能据此推断客户 Windows 设备的实际改善，也不能宣称 15.3 秒的问题已经解决。无真实授权、磁盘 I/O、杀毒或同步软件开销。以下旧版线程对照仅作历史记录，当前基准脚本增加了本节新旧算法对照。

### 内容校验线程（2026-09-05，历史版本）

用户复测的 235 张 / 1.5 GB 操作耗时 27.9 秒，其中内容校验累计 89.30 秒，系统删除累计 2.10 秒。这定位到校验调用等待，而非证明操作系统删除慢；累计时间包含并发重叠，不能直接相减来预测新版本用时。

SHA-256 仍用原生 WebCrypto，不改变算法或减少校验。现在由最多 4 个任务内 Worker 执行，使用 transferable ArrayBuffer 避免跨线程额外拷贝；每个 Worker 一次只处理一个完整文件，仍受原有 4 路 / 64 MiB 输入预算控制。Worker 没有文件系统句柄，不能删除文件。仅变更删除前的校验执行位置，扫描算法不变。

Worker 使用 Vite 的 inline 打包，避免妙搭 CDN 资源跨来源启动 Worker 失败。启动时以空输入的已知 SHA-256 自检；创建、加载或自检失败（2 秒启动超时）时，在传递任何照片数据前回退主线程完整计算。已转移数据后的异常、错误消息或 120 秒任务超时会使校验失败，不能批准删除，也不拿分离后的缓冲区重试。结束或故障后销毁 Worker，所有结果仍要通过原有指纹比较及停止检查。

完成页增加完整校验次数、累计校验数据（含保留项反复重查）、独立线程/兼容模式，以及计算调用累计用时。“内容校验”包含线程通信与等待，计算调用计时也不是纯 CPU 采样。不保存或上传任何性能信息。

### 非破坏性性能自检（2026-09-06）

用户在 Worker 版复测：成功 194 项、未删除 1 项，23.8 秒（含授权 2.11 秒）；390 次完整校验处理 2.6 GB，内容校验累计 69.18 秒，计算调用累计 68.29 秒，系统删除累计 2.27 秒。计算调用等待仍高，不能宣称问题已解决，也不能从浏览器版本号片段推断确定原因。

访问应用的 `/diagnostics` 路径，主动点击“开始自检”。单 Worker 原生 SHA-256、四 Worker 原生 SHA-256、四 Worker WASM SHA-256 分别完整处理 32 MiB 合成输入，两轮逆序对照，预热及启动不计入。每个输出必须与独立已知指纹一致；Worker 启动失败、错误指纹、运行异常、超时不计为有效速度结果。最多四路输入，每路 4 MiB；后台、离开或“停止自检”会取消运行并销毁 Worker。

自检不读图片、不触发目录授权、不执行删除、不保存或上传浏览器信息及结果。`hash-wasm@4.12.0` 仅在主动启动诊断时加载，许可随源码和诊断页面提供；候选实现不接入真实删除，也不会自动切换删除引擎。合成吞吐不代表实际磁盘删除用时。完成页独立展示“未删除文件及原因”，仅显示原始操作记录，不自动重试。

浏览器基准（纯本地开发页面，不进入应用发布入口）：

```sh
npx vite build --config tests/hash-benchmark.config.ts
npx vite preview --config tests/hash-benchmark.config.ts --host 127.0.0.1 --port 5300
# 浏览器访问 http://127.0.0.1:5300/tests/hash-benchmark.html 并点击运行
```

本机 Chromium 152、生产构建、交替顺序各测两次：

| 场景 | 主线程 | 4 个 Worker | 不变的工作量 |
| --- | --- | --- | --- |
| 6 MiB 合成输入 × 128 | 0.339 / 0.330 秒 | 0.101 / 0.100 秒 | 768 MiB 完整 SHA-256，结果一致 |
| 13 组 / 234 项内存模拟删除 | 1.433 / 1.388 秒 | 0.545 / 0.537 秒 | 468 次完整校验，共 2808 MiB；13 个保留项仍存在 |

以上比较保持同样的组并发、校验并发和逐次重查，只改变计算位置。文件系统适配器是内存 Map，没有真实授权、磁盘 I/O、杀毒或同步软件开销；不能把约 2.6 倍的模拟流程提升当作客户设备保证。已有 Node 延迟基准测的是另一种瓶颈，不应混用。

依据：[Chromium WebCrypto 实现](https://chromium.googlesource.com/chromium/src/+/HEAD/components/webcrypto/webcrypto_impl.cc) 中存在在调用线程执行 digest 的路径，故不能假设 Promise 即多核并行；具体路径取决于浏览器版本与配置。[MDN transferable objects](https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API/Transferable_objects)、[Vite Worker 打包](https://vite.dev/guide/features.html#web-workers)。

发布前后手动验收请仅使用专门复制的可丢弃测试目录：

1. 放入两份完全相同图片、一对相似图片和一个无关文件，从目录入口扫描。
2. 先拒绝写入权限，确认磁盘无变化；重新确认并允许，核对只有清单中的副本消失。
3. 改动一个被选文件或移走保留项后再确认，检查该项跳过且给出失败原因。
4. 多选图片入口的“删除文件”禁用；刷新结果页显示“暂无扫描结果”并提示重扫，没有恢复/保存提示。刷新完成页不恢复统计。
5. 测试部分失败/停止后查看明细；已成功删除的文件不应显示为可恢复。
6. 手机、Safari / Firefox 或内嵌浏览器如不支持原生目录授权，应仍可扫描查看，不能承诺真实删除可用。

接口依据：[Chrome File System Access 文档](https://developer.chrome.com/docs/capabilities/web-apis/file-system-access)、[MDN removeEntry](https://developer.mozilla.org/en-US/docs/Web/API/FileSystemDirectoryHandle/removeEntry)。
